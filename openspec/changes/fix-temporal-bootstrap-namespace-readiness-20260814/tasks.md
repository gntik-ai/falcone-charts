## 1. Reproduction and Black-Box Contract

- [x] 1.1 Add focused Helm-render and process-isolated black-box tests using the exact OpenSpec headers. Initial RED: 7 tests, 2 pass, 5 fail; follow-up REDs covered the exact consumer gate, revision identity, DNS truncation, producer/consumer drift, namespace and attribute races, malformed or wrong types, final read-back, and authoring compatibility. Final focal: 16/16 pass.
- [x] 1.2 Add compatibility coverage using `Existing values and Temporal state remain compatible across upgrade and rollback`: the exact five `Keyword` attributes remain valid in any order, divergent values fail before render, revision identity is retained, and no destructive cleanup is emitted.
- [x] 1.3 Add tests-only RED coverage for `Vanilla Kubernetes uses numeric image identities for every chart-owned Temporal container` and `OpenShift assigns arbitrary identities to every chart-owned Temporal container`, inventorying all custom Temporal Job/Deployment containers plus the workflow gate and semantically verifying the pinned images' effective users. Included in the second-block RED and final 37/37 GREEN. A later `bbx017` failure was a tests-only false expectation and was corrected to assert the explicit or inherited effective `1000:1000` identity without changing product code.
- [x] 1.4 Add tests-only RED coverage for `Inherited values cannot remove the mandatory Temporal bootstrap-state gate` and `User init containers remain additive to the mandatory Temporal gate`, including `--set-json ...=[]`, inherited/reused empty and replacement lists, multiple valid user entries, and a reserved-name collision. Included in the second-block RED and final 37/37 GREEN.
- [x] 1.5 Add process-isolated RED coverage for `Every terminal Temporal failure is namespace-specific bounded and fail-forward safe`, exercising frontend timeout, namespace verification, incompatible type, retry exhaustion, list failure, and final read-back drift in the reconciler and mandatory gate. Included in the second-block RED and final 37/37 GREEN.
- [x] 1.6 Add tests-only RED coverage for `Inherited Temporal security IDs are stripped from every OpenShift container`, injecting fixed IDs through both inherited Temporal security-context paths and inventorying server, Web UI, server gate, Job and workflow gate at pod/container scope. Included in the 28-pass/9-fail RED and final GREEN.
- [x] 1.7 Add render-negative RED coverage for `Workflow component identity cannot bypass the mandatory Temporal gate`, covering omitted and divergent inherited/reused/user `workflowWorker.wrapper.componentId` values plus the supported historical identity. Included in the 28-pass/9-fail RED and final GREEN.
- [x] 1.8 Add RED coverage for `Bootstrap Job and mandatory gate share the authoritative admin-tools image`: exact canonical defaults; `global.temporalAdminToolsImage` repository/tag/digest/pullPolicy override; global registry and normalized pull secrets; tri-state legacy compatibility; divergent legacy migration failure; reused historical custom legacy-without-global failure; and rejection of every workflow-specific or wrapper-fallback authority. Digest-qualified Job/gate parity and the tri-state fix are GREEN in the final focal.
- [x] 1.9 Add process-isolated RED coverage for `Every frontend and gate terminal branch reports namespace bound and fail-forward guidance`, requiring `falcone-flows`, exact phase/attribute, an explicit numeric bound, both guidance clauses and secret-safe output on every terminal branch. Included in the 28-pass/9-fail RED, intermediate 35-pass/2-fail state, and final GREEN.
- [x] 1.10 Add deterministic compact-metadata OCI coverage for `Pinned Temporal image metadata deterministically maps named users to numeric identities`, binding exact server, admin-tools and UI index/manifest/config artifacts. This metadata-only fixture and jq validation are GREEN; complete registry-retrieved filesystem reconstruction is separately proven in task 1.13.
- [x] 1.11 Correct and re-run focused black-box coverage using `Vanilla preserves supported positive identity overrides while OpenShift strips fixed identities`: `1000:1000` is asserted only for defaults without an applicable override; every supported positive UID/GID/pod-fsGroup override is asserted at its documented vanilla scope; OpenShift strips fixed identities; and both complete renders pass kubeconform. `bbx038` passes in 5.314 seconds in the authorized final checker focal.
- [x] 1.12 Add focused black-box coverage for `Legacy and global image equality normalizes absent and empty digests`, proving omitted and empty digests compare equal while an accepted non-empty canonical digest remains identical in Job and gate. `bbx039` and the complete final focal are GREEN for corrected tri-state normalization.
- [x] 1.13 Extend OCI coverage using `OCI reconstruction uses per-run cleanup and general whiteout semantics`: use a unique per-run temporary directory with guaranteed success/failure cleanup; prohibit the fixed 232 MiB cache; apply root and arbitrary-parent file/opaque whiteouts; and add adversarial synthetic replacement, deletion, opacity, and recreation fixtures. `bbx037` passes in 16932.641499 ms and proves all-layer reconstruction, general whiteouts, and cleanup.
- [x] 1.14 Extend operator coverage using `Job discovery cardinality failures include complete fail-forward context`: for zero and multiple history, stored-manifest, and live matches assert release, namespace, active revision, effective revision/lifecycle, full selector, cardinality, `correct or resolve`, and `retry to fail forward safely`. `bbx041` passes in 0.636228 ms, `bbx046` in 928.818334 ms, and `bbx050` in 4.484782 ms, proving complete diagnostics and effective stored-manifest selection.
- [x] 1.15 Add executable coverage for `Legacy image migration is copyable and linked from the chart README`, including direct link validity, copyable four-field migration, absent/empty digest normalization, and post-render Job/gate parity. `bbx042` and the complete final focal are GREEN for default/custom migration YAML, copyability, parity, and the chart-README link.
- [x] 1.16 Add a focused base-diff assertion for `Temporal remediation preserves unrelated Keycloak database initialization`, comparing with authoritative base `41922e9d` and failing on any `keycloak-db-init` drift. `bbx043` and the complete final focal are GREEN for byte-exact Keycloak isolation.
- [x] 1.17 Add adversarial render-negative coverage using `Templated user init container names cannot collide with the reserved Temporal gate`, proving a raw non-reserved name that evaluates through `tpl` to the reserved identity fails before workload rendering. The contract passes in the authorized 47/47 checker focal.
- [x] 1.18 Add actual stored-release coverage using `Real pre-global Helm stored values remain operable under reuse-values`: install the chart plus schema/values from base `41922e9d`; apply the new chart with real `--reuse-values`; prove exact-default legacy input without a stored global key passes schema and converges; prove custom legacy input reaches compatibility validation and fails closed until migrated. Live evidence on vCluster `ftb-a73c` proves default reuse parity/readiness, custom fail-closed without mutation, and successful explicit four-field migration. Offline `bbx048` passes in 1938.234286 ms and remains scoped only to stored-value coalescence and post-render capture, not actual apply.
- [x] 1.19 Add an executable positive/negative scope test using `Knative image pins are byte-exact against the authoritative base`: compare every Knative repository, tag, and digest with base `41922e9d`, then prove synthetic drift in each field fails. Dedicated positive/negative `bbx049` passes in 95.287784 ms.
- [x] 1.20 Enforce the evidence split using `Release-state acceptance tracks active revision Jobs across install upgrade retry and rollback`: focused tests restrict offline claims to predicted names/metadata/selectors and require disposable Helm transitions for active history, exact Job cardinality, retained immutable Jobs, and rollback behavior. The boundary contract passes; the transitions themselves remain pending in task 5.8.
- [x] 1.21 Add acceptance-ledger coverage using `Global and live acceptance remain mandatory publication gates`, preventing focused success from closing the unfiltered global suite, disposable live sequence, or subsequent independent re-review. The gate-state contract passes while tasks 5.7–5.9 correctly remain open.
- [x] 1.22 Add focused and live coverage using `Rollback Job discovery follows the deployed stored manifest metadata`: derive one active deployed history revision; parse exactly one bootstrap Job from that active revision's stored manifest; validate effective revision/lifecycle labels; select live using effective metadata; cover install/upgrade/retry/rollback replay, invalid labels, and stored/live cardinalities zero and two with complete active/effective diagnostics. `bbx050` passes in 4.484782 ms, and the bounded K3s rerun proves rollback active r3 correctly selects stored effective r1/install metadata with live cardinality one.
- [x] 1.23 Add tests-only RED coverage using `Long release names keep the workflow gate and Temporal frontend address identical`: render with an exact 53-character release name; assert the effective Temporal frontend Service DNS label is at most 63 characters; and require the bootstrap Job, mandatory workflow gate, and all Temporal consumers to use that exact hostname without an untruncated or divergent reconstruction. Authentic initial `bbx051` RED: 0/1, exit 1, test/total 528.675165/618.305496 ms; Service and Job expected `<53r>-temporal:7233`, while the gate rendered `<53r>-temporal-frontend:7233`.

## 2. Helm Lifecycle and Readiness

- [x] 2.1 Convert the Temporal bootstrap into an ordinary install/upgrade resource with no pre-install, post-install, pre-upgrade, or post-upgrade hook annotations, retaining Helm ownership, TTL cleanup, resource limits, image routing, and restricted security.
- [x] 2.2 Add a DNS-safe bootstrap Job identity whose retained suffix is derived from `.Release.Revision`; offline rendering proves predicted install, upgrade, retry, and rollback identities do not reuse the same immutable Job template. Actual active-release and retained-Job behavior remains pending in tasks 1.20 and 5.8.
- [x] 2.3 Complete the chart-owned mandatory gate path by comparing every user init-container's effective post-`tpl` name with the reserved identity before workload rendering; preserve additive order, inherited/reused empty and replacement-list safety, reserved-name rejection, and exact `workflow-worker` component identity. Adversarial focal coverage is GREEN.
- [x] 2.4 Apply and test the corrected identity matrix: default vanilla identities are image-compatible `1000:1000` only where no supported override applies; supported positive pod/container UID/GID and pod-fsGroup overrides remain effective at documented vanilla scopes; OpenShift strips every fixed identity after values/template resolution while retaining hardening; both complete renders pass kubeconform. `bbx038` is GREEN; live SCC proof remains task 5.8.
- [x] 2.5 Remove the unconditional schema requirement for authored `global.temporalAdminToolsImage` while keeping it the canonical Job/gate authority; allow real stored exact-default pre-global values from base `41922e9d` to reach compatibility validation and converge under `--reuse-values`; make custom legacy input without an equal canonical object fail closed with executable migration guidance; retain tri-state digest, registry, pull-secret, and workflow-specific-authority contracts. The disposable live sequence proves exact-default and custom-plus-migration behavior without state loss.
- [x] 2.6 Remove every unrelated `keycloak-db-init` product, test, helper, or documentation diff from this remediation and prove its template and rendered behavior remain byte-for-byte unchanged from base `41922e9d`. `bbx043`, the final 43/43 focal, and focused diffcheck prove isolation.
- [x] 2.7 Remove every unrelated Knative repository, tag, or digest change from this remediation and prove all Knative image pins remain byte-for-byte unchanged from base `41922e9d` through the dedicated positive/negative executable test in task 1.19. Definitive focal evidence is GREEN.
- [x] 2.8 Ensure bootstrap Job labels and annotations expose release, revision, and install/upgrade lifecycle inputs required by exact operator selection; keep offline claims limited to rendered metadata until the disposable release-state sequence proves active Jobs. Focused metadata and selector contracts are GREEN; actual active Jobs remain task 5.8.
- [x] 2.9 Centralize the effective Temporal frontend Service hostname in one chart helper with Helm-compatible `trunc 63` and trailing-hyphen trimming; make the Service, bootstrap Job, mandatory workflow gate, and all main Temporal consumers consume it identically; preserve short-release names and all existing ports, namespaces, readiness, security, and Temporal-state behavior. The first fix corrected the gate but left three main consumers divergent; independent checker RED remained 0/1 with test/total 541.894635/633.481873 ms. The second fix corrected those three values. Final checker `bbx051`: 1/1, exit 0, test/total 536.927845/622.319052 ms.

## 3. Idempotent Temporal Reconciliation

- [x] 3.1 Make frontend health exhaustion exit non-zero before namespace describe/create, naming the non-sensitive frontend target, bounded attempts/deadline, and a correct-or-resolve plus retry-to-fail-forward action.
- [x] 3.2 Reconcile the configured namespace race-safely: describe first, create only if missing, tolerate AlreadyExists only after successful verification, and never delete or recreate compatible existing state.
- [x] 3.3 Reconcile all five configured search attributes additively and verify every desired name and type after create or reuse; fail non-zero on create error, incompatible type, malformed output, or convergence drift without deleting attributes or workflow history.
- [x] 3.4 Make every terminal reconciler and mandatory-gate branch—including both frontend loops—name the configured namespace and failed phase/attribute, state an explicit numeric attempt/deadline bound, and include `correct or resolve` plus `retry to fail forward safely`, while keeping output free of response bodies, credentials, Secret values, environment dumps, and destructive rollback instructions. All focused terminal-branch contracts are GREEN.
- [x] 3.5 Execute clean-create, all-existing, partially-existing, AlreadyExists race, wrong-type, frontend-timeout, create/list failure, malformed output, late drift, and identical rerun cases against the rendered POSIX shell; 16/16 focused contracts prove only additive Temporal calls and stable fail-forward recovery.
- [x] 3.6 Re-run all failure modes after the diagnostic correction and prove every non-zero terminal path satisfies the same namespace/bound/guidance/secret-safety contract without adding a destructive Temporal call. Final focal 37/37 PASS.

## 4. Compatibility and Operator Guidance

- [x] 4.1 Preserve and render-test the established `falcone-flows`, retention, exact five attributes, canonical global admin-tools image, global registry/pull-secret routing, resources, deadlines, network, component identity, user init-container, pod and container security contracts without a data migration or new Secret/RBAC contract; accept exact-default/equal legacy values, fail closed on divergent or reused custom legacy images until migrated, accept attribute reordering, preserve user init additions, and prevent removal of the mandatory gate. Focused direct-render compatibility is GREEN; real stored/reused-value behavior remains pending in tasks 1.18 and 2.5.
- [x] 4.2 Correct the stale admin-tools post-hook comment, stale/current-revision Job selectors and link targets in operator commands, release-note qualification for the shared admin-tools init image/startup gate, and any obsolete completion claims in task text; document copyable migration of repository/tag/digest/pullPolicy from `temporal.adminTools.image` to `global.temporalAdminToolsImage`, retain bounded failure interpretation, safe retry, cleanup, and the prohibition on deleting shared Temporal state. Non-blocking documentation corrections are complete; independent re-review remains pending.
- [x] 4.3 Document rollback preconditions: verify the retained namespace is usable by the target version before rollback; otherwise correct the condition and retry forward; state that chart rollback never reverses Temporal namespace, history, or search-attribute state.
- [x] 4.4 Complete and execute the chart-README-linked operator procedure: accept release and namespace; invoke `helm history -n`; require exactly one deployed `ACTIVE`; read `helm get manifest --revision ACTIVE`; extract exactly one `component=temporal-bootstrap` Job; validate its effective positive revision and `install|upgrade` lifecycle; build the selector from effective stored-manifest labels; and make every history/manifest/live zero-or-multiple or invalid-label diagnostic include release, namespace, active revision, effective revision/lifecycle, selector, count, `correct or resolve`, and `retry to fail forward safely`. Retain migration and Job/gate parity guidance. `bbx041`, `bbx046`, `bbx050`, and the K3s rollback rerun prove the corrected procedure.
- [x] 4.5 Apply non-blocking documentation polish without changing procedure semantics: declare the operator runbook's actual prerequisites as Bash plus `awk` and `sed` rather than POSIX-shell compatibility, and move the default/custom YAML migration blocks into the Admin-tools section for coherent discovery. Polish is complete and the procedure semantics remain unchanged.

## 5. Validation and Independent Acceptance

- [x] 5.1 Run the pre-review JavaScript syntax, focused Temporal bootstrap suite, schema parsing, strict Helm lint, default/install/upgrade/OpenShift renders, rendered POSIX-shell syntax, manifest security/Secret scans, and `git diff --check`; 16/16 focused contracts passed, with only Helm's pre-existing informational icon recommendation.
- [x] 5.2 Obtain the first independent review. Verdict: `REQUEST_CHANGES`; blocking findings are incomplete vanilla/OpenShift identity handling, a values-replaceable mandatory gate, incomplete terminal fail-forward diagnostics, and missing global/disposable acceptance. Non-blocking corrections cover stale lifecycle comments, revision-specific Job selectors, and release-note qualification.
- [x] 5.3 Obtain the second independent review. Verdict: `REQUEST_CHANGES`; blockers are inherited OpenShift security IDs, component-ID gate bypass, divergent Job/gate image authorities, incomplete numeric namespace-specific frontend/gate diagnostics, non-deterministic/incomplete server-admin-tools-UI image-user evidence, and still-pending global/disposable acceptance. Non-blocking corrections cover stale comments, current-revision selectors/links, release wording and task status.
- [x] 5.4 Re-run the expanded reviewer-blocker focal suite after the second correction. Evidence progression: initial 37 tests with 28 pass/9 fail; intermediate corrections reached 35 pass/2 fail; final run passed 37/37, exit 0, duration 35.024 seconds. Lifecycle, component identity, mandatory/additive gate, global authoritative-image tri-state/digest plus legacy migration, deterministic OCI-user, vanilla/OpenShift inherited-identity, numeric diagnostics, security, compatibility, and rollback are GREEN; `node --check`, jq fixture validation and focused diff checks also PASS.
- [x] 5.5 Obtain the third independent review. Verdict: `REQUEST_CHANGES`; `CHAIN_STATUS: incomplete`. Its six blockers are supported UID/GID/fsGroup override behavior on vanilla/OpenShift plus kubeconform, absent/empty legacy/global digest normalization, raw cryptographically digest-bound OCI evidence, public install/upgrade/truncated-release Job selection, copyable chart-README-linked image migration, and removal of unrelated `keycloak-db-init` drift relative to base `41922e9d`.
- [x] 5.6 Run the six third-review focused contracts in tasks 1.11–1.16 and the relevant chart/schema/diff validations; capture RED-to-GREEN evidence without treating the earlier 37/37 focal run as proof of these new blockers. Pre-correction focal: 43 total, 36 pass, 7 fail, exit 1, duration 39.265 seconds. Intermediate focal: 43 total, 39 pass, 4 fail, exit 1, duration 40.981 seconds. Definitive focal: 43/43 pass, fail/skip/cancel 0, exit 0, duration 42.626 seconds. Literal kubeconform is clean for vanilla (`Valid 189 / Invalid 0 / Errors 0 / Skipped 26`, exit 0) and OpenShift (`Valid 188 / Invalid 0 / Errors 0 / Skipped 30`, exit 0); Node syntax, jq fixture validation, and focused diffcheck also PASS.
- [x] 5.6.1 Obtain the fourth independent review. Verdict: `REQUEST_CHANGES`; `CHAIN_STATUS: incomplete`. Its eight blockers are corrected vanilla-default/override/OpenShift semantics, effective post-`tpl` reserved-name collision rejection, all-layer whiteout-aware OCI evidence, real pre-global reused-values compatibility, fully executable namespaced exact-cardinality Job discovery, removal of unrelated Knative pin drift, actual release-aware install/upgrade/retry/rollback evidence or narrowed offline claims, and still-pending global/live acceptance.
- [x] 5.6.2 Add and run the eight fourth-review literal contracts in tasks 1.11, 1.13–1.14, and 1.17–1.21 plus the reopened implementation/operator tasks; capture new RED-to-GREEN evidence without treating the third-review 43/43 focal as proof. Initial RED: 47 total, 41 pass, 6 fail, exit 1, duration 44.784 seconds; its `bbx045` regex was corrected tests-only after the run. After maker corrections, the independent checker's first focal reached 46/47 with only a tests-only `bbx047` grammar regex (`do not` versus `does not`); after that adjustment, the authorized repetition passed 47/47, exit 0, duration 52.778 seconds. `bbx037` all-layer reconstruction passes in 6.753 seconds; `bbx038` vanilla/OpenShift kubeconform passes in 5.314 seconds; `bbx044`–`bbx047` all pass. Node syntax, jq, lint, and focused diffcheck also PASS. Global, live, re-review, and final bookkeeping remain tasks 5.7–5.10.
- [x] 5.6.3 Obtain the fifth independent review. Literal verdict: `VERDICT REQUEST_CHANGES`; `CHAIN_STATUS incomplete`. Blockers: schema-required `global.temporalAdminToolsImage` breaks real base-`41922e9d` stored values under `--reuse-values` and `bbx045` was not a stored-release test; OCI leaves a fixed 232 MiB cache and misses root/arbitrary-parent whiteouts; zero/multiple selector diagnostics omit release, namespace, revision, lifecycle, selector, and corrective fail-forward action because the test checked count only; task 1.19 claims byte-exact Knative isolation without an executable test.
- [x] 5.6.4 Add and run the four fifth-review literal contracts in tasks 1.13–1.14 and 1.18–1.19 plus implementation/operator tasks 2.5, 2.7, and 4.4. Definitive offline focal: 50/50 pass, exit 0, duration 64618.26073 ms. Exact checker timings: `bbx037` 16932.641499 ms; `bbx041` 0.636228 ms; `bbx046` 928.818334 ms; `bbx048` 1938.234286 ms; `bbx049` 95.287784 ms; `bbx050` 4.484782 ms. Helm 4 post-renderer capture proves stored-Secret/coalescence behavior without mutation, while OCI cleanup and executable Knative scope checks are GREEN.
- [x] 5.6.5 Correct and run the literal rollback-discovery contract in task 1.22: prove stored-manifest effective metadata for install, upgrade, retry/fail-forward and rollback, plus invalid labels and zero/two history-manifest-live cardinalities with complete diagnostics. `bbx050` and the bounded K3s rollback rerun are GREEN.
- [x] 5.7 Complete the final unfiltered global acceptance record. The earlier post-live diagnostic run remained non-authoritative at 483/484 and led to completed baseline task 5.7.1. The final authoritative gate invoked exactly one `bash tests/blackbox/run.sh`: 20 files, 484/484 pass, fail/skip/cancel/todo 0, exit 0, duration 1733813.435611 ms, overall black-box PASS. Temporal passed 50/50 (`bbx001`–`bbx050`, including `bbx046`, `bbx048`, and `bbx050`). `bbx-8-003` passed in 15678.619203 ms against baseline `330c39...`. Branch/HEAD before and after were identical at `fix/temporal-bootstrap-namespace-readiness @ 41922e9d...`; NUL status was byte-identical at 2137 bytes with SHA-256 `35a09a...`; the discovered list contained 20 files, 1622 bytes, SHA-256 `0d5fa2...`; all test paths occupied 3294 bytes with SHA-256 `01f5b1...`. The run left no artifacts or edits, did not mutate a cluster, and produced no Playwright output.
- [x] 5.7.1 Reproduce and reconcile `bbx-8-003`. Before the fixture edit, three isolated processes selected by exact title produced identical actual SHA-256 `330c39fcdbae7ebc9edc4f4f3a4a408c849445cf8fe32567a0369c73d93531ee` against expected `66460fb...d97ad1`: run 1 test/total 15574.901252/15645.672184 ms; run 2 15176.836961/15245.351975 ms; run 3 15830.621842/15900.78587 ms. Default and explicit-disabled renders were byte-identical; both contained zero `falcone.knative-runtime/lifecycle` and zero `KNATIVE_RUNTIME_MODE`; raw SHA `cc111e...` was informational; packaged `.tgz` count was zero; ESO `Chart.lock` `e25decc...` remained preserved. Only the authoritative fixture was updated. Post-update focused result: 1/1 pass, exit 0, test duration 15533.863897 ms, total duration 15601.320765 ms; Node syntax, formatting, and focused diffcheck PASS. Task 5.7 remains pending for the separately authorized final global acceptance run.
- [x] 5.8 Complete disposable live acceptance. The initial `default`/vCluster `ftb-a73c` sequence proved real stored-value compatibility, r1–r6 install/upgrade/bounded-failure/fail-forward/rollback/restore state preservation, readiness, no Secrets, cleanup, and staging isolation while exposing the rollback selector defect. The bounded rerun used Kubernetes context exactly `default` on K3s, vCluster `frb-b91e`, host namespace `falcone-tb-rollback-20260814-b91e`: current install r1 rendered bootstrap TTL 3600; upgrade r2 succeeded; rollback created active r3 whose stored manifest exposed effective r1/install metadata; the corrected procedure selected exactly one live Job; all five `Keyword` attributes remained; workflow readiness was 2/2; evidence contained no Secrets; cleanup completed; staging remained intact. The literal harness block itself did not run only because the harness prohibits `rm -f`; the same commands passed when cleanup used the harness-approved `find` deletion. OpenShift live is N/A and is not claimed because this was K3s; vanilla/OpenShift render and kubeconform evidence are already GREEN.
- [x] 5.9 Obtain final independent re-review approval after the voluntarily promoted long-release hostname correction and documentation polish. Exact post-delta result: `VERDICT: APPROVE`, `BLOCKING: none`, `CHAIN_STATUS: complete`. Reviewer evidence covers the shared helper in the mandatory gate and all three main consumers; exact 53-character release parity across Service, Job, gate, control-plane, executor, and workflow with DNS length at most 63; unchanged short-release behavior; `bbx051` RED→RED→GREEN; Temporal 51/51; baseline 3/3; global 485/485; Bash plus `awk`/`sed` prerequisites and relocated YAML migration blocks; and no security, scope, Knative, or Keycloak drift.
- [x] 5.9.1 After tasks 1.23, 2.9, and 4.5, re-run `bbx051` and the complete Temporal focal. Final checker `bbx051`: 1/1, exit 0, test/total 536.927845/622.319052 ms. Single complete focal: 51/51, exit 0, duration 65514.005973 ms; exact tests `bbx051` 442.551692 ms, `bbx037` 16686.062127 ms, `bbx041` 0.604961 ms, `bbx046` 687.089633 ms, `bbx048` 2429.451013 ms, `bbx049` 82.517909 ms, and `bbx050` 3.947075 ms. Node syntax twice, jq, OCI cache-absence assertion, lint, strict OpenSpec, and focused diffcheck all PASS.
- [x] 5.9.2 Obtain a fresh independent re-review of the hostname-helper and documentation-only delta after baseline reconciliation and the post-delta global gate. Exact result: `VERDICT: APPROVE`, `BLOCKING: none`, `CHAIN_STATUS: complete`; all reviewer evidence is recorded in task 5.9.
- [x] 5.9.3 Reconcile deterministic baselines after the long-release helper. The authoritative baseline did not change. Three isolated processes selected by exact title all passed with fixture SHA-256 `330c39fcdbae7ebc9edc4f4f3a4a408c849445cf8fe32567a0369c73d93531ee`: run 1 test/total 15201.939238/15271.300684 ms; run 2 14655.220995/14725.196037 ms; run 3 15209.560557/15279.413034 ms. Every run proved default and explicit-disabled byte-identical with zero `KNATIVE_RUNTIME_MODE` and zero `falcone.knative` wiring. Reconciliation was a no-op and did not write the fixture. One final focused run passed 1/1 with test/total 15022.737141/15091.030355 ms. Packaged `.tgz` count was zero, ESO `Chart.lock` `e25decc...` remained intact, and Node syntax, formatting, expected-value, and focused diff checks PASS.
- [x] 5.9.4 Run exactly one authorized unfiltered post-delta `bash tests/blackbox/run.sh`. Final post-delta gate: 20 files, 485/485 pass, fail/skip/cancel/todo 0, exit 0, duration 1736568.600687 ms, overall PASS. Temporal remained 51/51 GREEN; `bbx051` passed in 443.433569 ms; `bbx-8-003` passed in 15258.86027 ms against baseline `330c39...`. Branch/HEAD before and after were identical at `fix/temporal-bootstrap-namespace-readiness @ 41922e9d...`; NUL status was byte-identical at 2137 bytes with SHA-256 `35a09a...`; discovery contained 20 files/1622 bytes with SHA-256 `0d5fa2`; the complete fileset was 3294 bytes with SHA-256 `01f5b1`. No artifacts or edits remained and no cluster mutation occurred.
- [x] 5.10 Complete final bookkeeping and validation. The final ledger contains 63 completed tasks and zero pending tasks. The delta spec contains 10 requirements and 39 unique scenario headers, including the literal long-release contract, with no duplicate headers. `openspec validate fix-temporal-bootstrap-namespace-readiness-20260814 --strict` PASS and repository-wide `git diff --check` PASS after final task status update.
