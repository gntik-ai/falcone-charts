## 1. Add Black-Box Contract Coverage

- [x] 1.1 Add a Helm-render test labeled `Fresh install sends Keycloak 26 login settings as top-level realm fields` that asserts all five configured booleans are top-level in `realm.json`, the `login` wrapper is absent, and unrelated realm properties remain intact. Focused contract: GREEN.
- [x] 1.2 Add a process-isolated test labeled `Existing realms are preserved while Keycloak 26 login settings are reconciled on upgrade` that executes the rendered bootstrap program with a matching marker and fake Keycloak HTTP interface, proves exact PUT/read-back convergence, proves identical rerun behavior, and observes no realm recreation or deletion. Focused contract: GREEN.
- [x] 1.3 Add render-failure tests labeled `Malformed realm login configuration fails before any Keycloak mutation` and `Conflicting nested and top-level realm login settings fail closed`, including equal duplicates, differing conflicts, explicit `login: null`, and top-level-only replacement attempts. Reviewer tests bbx012/013 first produced the required tests-only RED result (19 total, 17 pass, 2 fail) and pass after the correction with no partial manifest or unsafe diagnostic.
- [x] 1.4 Add process-isolated failure tests labeled `Realm bootstrap failures do not expose credentials or Secret values` and `Realm compatibility failure does not roll back or continue a partial bootstrap`, using sentinel credentials to assert bounded output, non-zero exit, PUT/GET/drift and initial GET/POST transport/status gates, no later verification, no marker write, no destructive realm operation, and identical retry behavior. Expanded focused contract: GREEN.
- [x] 1.5 Add reviewer-blocker tests bbx020-bbx027. Tests-only RED before the fixes: 27 total, 19 pass, 8 fail, exit 1, duration `18853.28513 ms`. Coverage uses non-zero `failureFactor: 99` versus nested `10` for merge precedence and requires realm ID plus correct/resolve/retry/fail-forward guidance for discovery, create, login PUT/GET, and drift failures while preserving bounded secret-safe output and no DELETE/rollback.

## 2. Render the Compatible Realm Payload

- [x] 2.1 Update the realm payload renderer to omit `login`, lift only `loginWithEmailAllowed`, `registrationAllowed`, `rememberMe`, `verifyEmail`, and `resetPasswordAllowed` into the top-level create representation, and preserve the existing brute-force lift and other realm properties.
- [x] 2.2 Render a minimal `login.json` reconciliation payload containing the realm identifier and exactly the five top-level booleans, with no nested `login` object or credential-bearing content.
- [x] 2.3 Require `bootstrap.oneShot.keycloak.realm.login` to be present and non-null in the schema, retain its five required booleans and no-additional-property validation, and independently reject any of the five direct top-level login fields even when the nested object is absent or null; emit path-specific diagnostics without echoing configured values.
- [x] 2.4 Replace the reviewer-rejected `mergeOverwrite` normalization with two historical non-overwriting merge phases—base realm plus brute-force partial, then login partial—so an unrelated non-zero top-level realm value retains precedence; verify with bbx020's numeric `99` versus `10` oracle rather than a boolean false value.

## 3. Reconcile and Gate Keycloak Bootstrap

- [x] 3.1 Add an idempotent Keycloak realm-login reconcile operation that applies the minimal payload to an existing realm, accepts PUT 200/204, reads it back, compares all five properties exactly, and fails on transport errors, unexpected statuses, or drift without deleting or recreating realm state.
- [x] 3.2 Invoke login reconciliation unconditionally from the upgrade-safe phase even when a matching one-shot marker skips create-only work, and require successful convergence before later reconciliation, final verification, or marker creation; process-isolated marker/idempotency evidence is GREEN.
- [x] 3.3 Make realm create/reconcile failure diagnostics bounded and secret-safe: do not print Authorization material, Secret-sourced values, credential environment, decoded Secrets, or untrusted response bodies that could echo sensitive data.
- [x] 3.4 Execute fresh process-isolated routes for GET 404 with POST 201 and concurrent POST 409, requiring login PUT/read-back, later authentication verification, and the sole marker write in that order. Execute initial realm GET and create transport/status failures twice each to prove bounded errors, no destructive action, no later phase or marker, and identical retries while no marker exists. The complete live Job remains task 5.3.
- [x] 3.5 Add the target realm identifier and bounded actionable `correct or resolve` plus `retry to fail forward safely` guidance to realm discovery/create and login PUT/GET/drift transport/status diagnostics without printing bodies or secrets and without issuing DELETE or rollback. Independent post-fix focal: exactly one run, 27/27 pass, exit 0, duration `19027.946618 ms`; `node --check` ×3 and diff check PASS.

## 4. Document Recovery and Compatibility

- [x] 4.1 Update install, upgrade, and release guidance with the Keycloak 26 compatibility symptom, the five-property rendered shape, safe status/marker checks, and commands for deterministic and disposable verification without embedding credentials.
- [x] 4.2 Document conditional rollback versus fail-forward: preserve existing realms, never claim automatic realm rollback, require failed fresh installs to correct and retry, state rollback preconditions for an already usable realm, and verify authentication plus marker state after recovery.

## 5. Verify the Change

- [x] 5.1 Run schema validation, Helm lint/renders, rendered Bash syntax, strict diff checks, and the focused black-box suite. Historical evidence: 11/11 GREEN, three identical baseline renders at digest `6aff492c5d342157e0205e259a2c62a8f7f2eb5ba4342174b56de95550a8b157`, focal comparison 1/1 PASS, and clean sentinel scans. Reviewer TDD evidence: tests-only RED at 19 total/17 pass/2 fail for bbx012/013 before the correction; the single expanded post-fix focal passed 19/19 with fail/cancelled/skipped/todo 0, exit 0, duration `14599.367914 ms`; `node --check` ×3 and `git diff --check` PASS.
- [x] 5.2 Run the payload/API portion of `Disposable Keycloak 26 verification imports the rendered realm and authenticates the bootstrap principal` in a disposable namespace on the default K3s context. Evidence: pre-fix POST 400 `unable to read contents from stream`; normalized control POST 201; post-fix exact render POST 201, PUT 204 twice, exact five-field read-back preserving realm/display/enabled/brute-force state, bootstrap-admin authentication 200, disposable namespace/Secret/workload/state removed, and shared staging unchanged.
- [ ] 5.3 **BLOCKED — independently confirmed E6 high; archive/release gate, not a draft-PR blocker:** Run the complete Helm clean-install bootstrap Job against disposable Keycloak 26 and prove terminal Job success, the success marker is written only after full authentication verification, marker-backed retry behavior is correct, evidence is sanitized, and cleanup is complete. Defect `P18:helm-clean-install:temporal-post-install-namespace-readiness-cycle` leaves chart `0.4.19` release revision 1 `pending-install`: `workflow-worker` is 0/2 because it requires `falcone-flows`, whose sole creator is a post-install Temporal hook that never starts under `--wait`. Keycloak and Temporal frontend are 1/1, but no Keycloak bootstrap hook or marker is reached. This independent blocker will be remediated separately and does not complete or invalidate the Keycloak task.
- [x] 5.4 Run the authoritative full black-box gate exactly once and unfiltered as `bash tests/blackbox/run.sh` on the pre-reviewer-correction revision. Result: 19 files, 418/418 pass, fail/cancelled/skipped/todo all 0, exit 0, duration `1646120.800029 ms`; worktree status was identical before and after and no run artifacts remained. Retain this as historical evidence; task 5.7 is the required current-diff rerun.
- [x] 5.5 Obtain final independent reviewer approval. Result: `VERDICT: APPROVE`, `BLOCKING: none`, `CHAIN_STATUS: complete`. The reviewer confirmed restored historical precedence (`failureFactor: 99` over nested `10`), bounded realm-specific correct/resolve/retry/fail-forward diagnostics, independent focal 27/27, and authoritative global 434/434. The sandbox could not rerun `bwrap` because user namespaces were unavailable, but chart lint, strict OpenSpec validation, rendered Bash, schema, JavaScript syntax, diff checks, and the baseline focal passed; the namespace restriction was not treated as a product failure.
- [x] 5.6 Run `openspec validate fix-keycloak-26-realm-login-import --strict` and an OpenSpec-only `git diff --check`; resolve every artifact-format or whitespace failure before handoff.
- [x] 5.7 Run exactly one unfiltered `bash tests/blackbox/run.sh` against the reviewer-corrected current diff. Result: 19 files, 426/426 pass, fail/cancelled/skipped/todo all 0, exit 0, duration `1661234.020453 ms`; Keycloak 19/19 with bbx012/013 PASS. Branch/HEAD were identical pre/post at `fix/keycloak-26-realm-login-import` / `8f6311741d137cb1f9a7571dd02073c8b0a89b67`; porcelain v1 NUL with all untracked files was 964 bytes, SHA-256 `65b672eb8f5c14e48448960b358e90750ce79b9ef41183aa882a13ef0fc73d34`, pre/post. No artifacts, edits, or cluster access occurred.
- [x] 5.8 Run exactly one final unfiltered, no-retry `bash tests/blackbox/run.sh` after the baseline update. Result: 19 files, 434/434 pass, fail/cancelled/skipped/todo all 0, exit 0, duration `1669954.154576 ms`, `black-box: PASS`; `bbx-8-003` PASS against baseline `f2fa87f5785e25534ddb4c97013ece1464f2f8fd098a527834e6f178f5929cbf`; Keycloak 27/27 including bbx020-bbx027 PASS. Branch/HEAD were identical pre/post at `fix/keycloak-26-realm-login-import` / `8f6311741d137cb1f9a7571dd02073c8b0a89b67`; NUL porcelain status was 964 bytes with SHA-256 `65b672eb8f5c14e48448960b358e90750ce79b9ef41183aa882a13ef0fc73d34` pre/post. No artifacts, edits, cluster access, or Playwright occurred.
- [x] 5.9 Validate the new baseline before and after the sole fixture update. Before editing, run three isolated processes against an overlay candidate `f2fa87f5785e25534ddb4c97013ece1464f2f8fd098a527834e6f178f5929cbf`; each passed 1/1 in `15473.378687 ms`, `15476.613765 ms`, and `15461.298514 ms`, proving default equals explicitly disabled, zero optional wiring, and source archives unchanged. Update exactly one fixture, then run the real focal once: 1/1 PASS, exit 0, duration `15187.96846 ms`; JavaScript syntax, formatting, and diff checks PASS.
