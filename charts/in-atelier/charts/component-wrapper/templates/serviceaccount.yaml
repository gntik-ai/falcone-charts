{{- if and .Values.enabled .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "component-wrapper.serviceAccountName" . }}
  labels:
    {{- include "component-wrapper.labels" . | nindent 4 }}
{{- end }}
