deployment:
  profile: standard
apisix:
  replicas: 2
keycloak:
  replicas: 1
kafka:
  # Kafka HA is not enabled by this profile: the generic component-wrapper cannot
  # provide per-pod KRaft node IDs/quorum voters or per-pod PVCs yet.
  replicas: 1
controlPlane:
  replicas: 2
webConsole:
  replicas: 2
