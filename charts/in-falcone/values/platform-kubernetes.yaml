platform:
  target: kubernetes
  network:
    exposureKind: Ingress
  securityProfile: restricted
  openshift:
    enabled: false
  routeAnnotations:
    kubernetes.io/ingress.class: nginx

publicSurface:
  ingress:
    className: nginx
    annotations:
      kubernetes.io/ingress.class: nginx
