{{- /*
  Falcone creates/adopts <release>-seaweedfs-db-secret from the parent chart's
  pre-install credential bootstrap hook. Do not render the upstream generated
  password here: Helm manifests must not expose live install credentials.
*/ -}}
