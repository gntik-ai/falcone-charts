{{- /*
  The gateway shared Secret is created/adopted by templates/platform-credentials.yaml.

  This file intentionally renders no Secret: the value must be generated inside
  the cluster so Helm output never contains GATEWAY_SHARED_SECRET.
*/ -}}
